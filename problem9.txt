**Problem 9: Print Information About a Given File**

### Aim

Write a program to print the following information about a given file:

a. Inode
b. Number of hard links
c. UID
d. GID
e. Size
f. Block size
g. Number of blocks
h. Time of last access
i. Time of last modification
j. Time of last change

### Concept

The `stat()` system call is used to obtain information about a file.

It stores the file information in a `struct stat` structure.

Important members of `struct stat` are:

* `st_ino` → Inode number
* `st_nlink` → Number of hard links
* `st_uid` → User ID (UID)
* `st_gid` → Group ID (GID)
* `st_size` → File size in bytes
* `st_blksize` → Block size
* `st_blocks` → Number of blocks allocated
* `st_atime` → Time of last access
* `st_mtime` → Time of last modification
* `st_ctime` → Time of last status change

The `ctime()` function converts the time value into a readable date and time format.

### Program

The program is stored in `problem9.c`.

```c
#include <stdio.h>
#include <sys/stat.h>
#include <time.h>

int main() {
    struct stat file;

    if (stat("input.txt", &file) == -1) {
        perror("stat");
        return 1;
    }

    printf("Inode number       : %ld\n", file.st_ino);
    printf("Number of links    : %ld\n", file.st_nlink);
    printf("UID                : %d\n", file.st_uid);
    printf("GID                : %d\n", file.st_gid);
    printf("Size               : %ld bytes\n", file.st_size);
    printf("Block size         : %ld bytes\n", file.st_blksize);
    printf("Number of blocks   : %ld\n", file.st_blocks);

    printf("Last access        : %s", ctime(&file.st_atime));
    printf("Last modification  : %s", ctime(&file.st_mtime));
    printf("Last change        : %s", ctime(&file.st_ctime));

    return 0;
}
```

### Compilation

Compile the program using:

```bash
gcc problem9.c -o problem9
```

### Create a Test File

```bash
echo "Hello Linux" > input.txt
```

### Execution

```bash
./problem9
```

### Sample Output

```text
Inode number       : 123456
Number of links    : 1
UID                : 1000
GID                : 1000
Size               : 12 bytes
Block size         : 4096 bytes
Number of blocks   : 8
Last access        : Fri Sep 18 10:20:30 2026
Last modification  : Fri Sep 18 10:20:30 2026
Last change        : Fri Sep 18 10:20:30 2026
```

The actual inode number, UID, GID, timestamps, and block information will depend on the system and the file.

### Explanation

1. `struct stat file;` creates a structure to store file information.

2. `stat("input.txt", &file)` obtains information about `input.txt` and stores it in the `file` structure.

3. `file.st_ino` gives the inode number.

4. `file.st_nlink` gives the number of hard links to the file.

5. `file.st_uid` gives the UID of the owner.

6. `file.st_gid` gives the GID of the group.

7. `file.st_size` gives the size of the file in bytes.

8. `file.st_blksize` gives the preferred block size for the file system.

9. `file.st_blocks` gives the number of blocks allocated to the file.

10. `file.st_atime` gives the time when the file was last accessed.

11. `file.st_mtime` gives the time when the file contents were last modified.

12. `file.st_ctime` gives the time when the file's metadata/status was last changed.

13. `ctime()` converts the time values into a human-readable format.

### Important Note

`st_ctime` does **not** mean file creation time. It represents the time of the last change to the file's metadata/status, such as permissions, ownership, or link count.

### Cleanup

After testing, remove the executable and test file:

```bash
rm -f problem9 input.txt
```

### Git Commands

```bash
git add problem9.c problem9.txt
git commit -m "Add program to display file information"
git push
```

